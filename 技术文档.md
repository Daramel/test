# 企业征信管理平台技术设计文档

## 文档信息

| 项目 | 内容 |
|-----|------|
| 文档名称 | 企业征信管理平台技术设计文档 |
| 版本号 | V1.0 |
| 编写日期 | 2026-06-26 |
| 文档状态 | 初稿 |

---

## 一、技术选型

### 1.1 总体技术架构

采用**前后端分离 + 微服务架构**，保证系统的可扩展性和可维护性。

### 1.2 技术栈选型

#### 前端技术栈

| 分类 | 技术选型 | 版本 | 选型理由 |
|-----|---------|------|---------|
| **框架** | Vue 3 | 3.x | 渐进式框架，生态成熟，学习曲线平缓 |
| **语言** | TypeScript | 5.x | 类型安全，提升代码可维护性 |
| **UI组件库** | Element Plus | 2.x | 企业级UI组件库，功能全面 |
| **状态管理** | Pinia | 2.x | Vue官方推荐，轻量高效 |
| **路由** | Vue Router | 4.x | Vue官方路由方案 |
| **HTTP客户端** | Axios | 1.x | 功能全面，拦截器支持好 |
| **图表库** | ECharts | 5.x | 百度开源，图表类型丰富 |
| **构建工具** | Vite | 5.x | 启动快，热更新快 |
| **CSS预处理器** | Sass | 1.x | 功能强大，嵌套语法 |
| **PDF预览** | PDF.js | 3.x | Mozilla开源，稳定可靠 |
| **代码规范** | ESLint + Prettier | 最新 | 代码质量和格式统一 |

#### 后端技术栈

| 分类 | 技术选型 | 版本 | 选型理由 |
|-----|---------|------|---------|
| **语言** | Java | 17 (LTS) | 生态成熟，企业级应用首选 |
| **框架** | Spring Boot | 3.x | 快速开发，约定优于配置 |
| **微服务** | Spring Cloud Alibaba | 2023.x | 国内主流，组件丰富 |
| **ORM** | MyBatis-Plus | 3.5.x | 增强版MyBatis，CRUD高效 |
| **数据库连接池** | HikariCP | 5.x | 性能最优的连接池 |
| **缓存** | Redis | 7.x | 高性能缓存，支持多种数据结构 |
| **搜索引擎** | Elasticsearch | 8.x | 全文检索，支持复杂查询 |
| **消息队列** | RabbitMQ | 3.x | 可靠消息投递，支持多种模式 |
| **任务调度** | XXL-Job | 2.4.x | 分布式任务调度，可视化管理 |
| **接口文档** | Knife4j | 4.x | Swagger增强，文档更友好 |
| **权限框架** | Spring Security + JWT | 6.x | 安全认证授权 |
| **工具库** | Hutool | 5.x | Java工具集，功能全面 |
| **验证码** | Easy-Captcha | 1.6.x | 图形验证码生成 |

#### 数据库选型

| 数据库 | 版本 | 用途 |
|-------|------|------|
| **MySQL** | 8.0 | 主数据库，存储业务数据 |
| **Redis** | 7.0 | 缓存、会话、排行榜、限流 |
| **Elasticsearch** | 8.x | 企业全文检索、日志检索 |
| **MongoDB**（可选） | 6.x | 非结构化数据存储（报告文件元数据） |

#### DevOps技术栈

| 分类 | 技术选型 | 版本/说明 |
|-----|---------|----------|
| **容器化** | Docker | 最新稳定版 |
| **容器编排** | Kubernetes | 1.28+ |
| **CI/CD** | Jenkins / GitLab CI | — |
| **服务注册发现** | Nacos | 2.x |
| **网关** | Spring Cloud Gateway | 3.x |
| **配置中心** | Nacos Config | 2.x |
| **链路追踪** | SkyWalking | 9.x |
| **监控告警** | Prometheus + Grafana | — |
| **日志收集** | ELK Stack (Elasticsearch + Logstash + Kibana) | — |

### 1.3 第三方服务集成

| 服务类型 | 服务商 | 用途 |
|---------|-------|------|
| **微信支付** | 微信支付商户号 | 在线支付 |
| **支付宝** | 支付宝商户号 | 在线支付 |
| **短信服务** | 阿里云短信 / 腾讯云短信 | 验证码、通知短信 |
| **邮件服务** | 企业邮箱 / 阿里云邮件推送 | 通知邮件、电子发票 |
| **电子签名** | 法大大 / e签宝 | 授权协议电子签名 |
| **电子发票** | 百望云 / 航天信息 | 电子发票开具 |
| **企业数据源** | 天眼查 / 企查查 / 启信宝 | 企业工商、司法数据 |
| **对象存储** | 阿里云 OSS / 腾讯云 COS | 报告文件、图片存储 |

---

## 二、系统架构设计

### 2.1 总体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户访问层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   PC端管理后台 │  │  移动端H5    │  │  开放API     │  │  数据大屏    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              网关层                                     │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Spring Cloud Gateway  (路由、限流、鉴权、日志、跨域)            │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            业务服务层                                   │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 用户认证  │ │ 企业信息  │ │ 信用评估  │ │ 报告服务  │ │ 订单支付  │   │
│  │  服务    │ │  服务    │ │  服务    │ │  服务    │ │  服务    │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 授权管理  │ │ 风险预警  │ │ 黑名单   │ │ 统计分析  │ │ 系统管理  │   │
│  │  服务    │ │  服务    │ │  服务    │ │  服务    │ │  服务    │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            数据支撑层                                   │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │  MySQL   │ │  Redis   │ │   ES     │ │  RabbitMQ│ │   OSS    │   │
│  │  主库    │ │  缓存    │ │  搜索    │ │  消息队列│ │  对象存储│   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            外部服务层                                   │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 企业数据  │ │ 微信支付  │ │ 支付宝   │ │  短信    │ │  电子签名 │   │
│  │  源      │ │          │ │          │ │  服务    │ │  服务    │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 微服务划分

| 服务名称 | 服务编码 | 端口范围 | 职责描述 |
|---------|---------|---------|---------|
| **网关服务** | credit-gateway | 8080 | 统一入口，路由转发、限流、鉴权、日志 |
| **用户认证服务** | credit-auth | 8081 | 用户注册、登录、认证、授权签署 |
| **企业信息服务** | credit-enterprise | 8082 | 企业信息查询、股东、高管、工商变更 |
| **信用评估服务** | credit-evaluate | 8083 | 信用评分、评级、评估模型管理 |
| **报告服务** | credit-report | 8084 | 报告生成、模板管理、报告预览下载 |
| **订单支付服务** | credit-order | 8085 | 订单管理、支付集成、退款、发票 |
| **授权管理服务** | credit-grant | 8086 | 企业间报告授权、授权管理 |
| **风险预警服务** | credit-risk | 8087 | 风险监控、预警规则、预警通知 |
| **黑名单服务** | credit-blacklist | 8088 | 黑名单管理、灰名单、名单核验 |
| **统计分析服务** | credit-statistics | 8089 | 数据统计、报表生成、数据大屏 |
| **系统管理服务** | credit-system | 8090 | 用户管理、角色权限、字典、日志 |

### 2.3 服务间调用关系

```
网关 (Gateway)
    │
    ├──→ 认证服务 (Auth) ←── 系统管理服务 (System)
    │        │
    │        └── 调用用户信息
    │
    ├──→ 企业信息服务 (Enterprise)
    │        │
    │        └── 调用ES搜索、MySQL查询
    │
    ├──→ 信用评估服务 (Evaluate)
    │        │
    │        └── 调用企业信息服务、模型计算
    │
    ├──→ 报告服务 (Report)
    │        │
    │        ├── 调用信用评估服务
    │        ├── 调用企业信息服务
    │        └── 调用OSS存储
    │
    ├──→ 订单支付服务 (Order)
    │        │
    │        ├── 调用报告服务（支付成功触发生成）
    │        ├── 调用微信/支付宝
    │        └── 调用余额账户
    │
    ├──→ 授权管理服务 (Grant)
    │        │
    │        └── 调用报告服务（授权报告查看）
    │
    ├──→ 风险预警服务 (Risk)
    │        │
    │        ├── 调用企业信息服务
    │        ├── 调用消息通知
    │        └── 调用XXL-Job定时任务
    │
    ├──→ 黑名单服务 (Blacklist)
    │        │
    │        └── 调用企业信息核验
    │
    ├──→ 统计分析服务 (Statistics)
    │        │
    │        └── 汇总各服务数据
    │
    └──→ 系统管理服务 (System)
             │
             └── 用户、角色、权限、字典、日志
```

---

## 三、核心模块技术设计

### 3.1 企业认证与授权签署模块

#### 3.1.1 模块架构

```
┌─────────────────────────────────────────┐
│         认证授权服务 (credit-auth)       │
├─────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐      │
│  │ 企业认证模块 │  │ 登录认证模块 │      │
│  │  - 注册     │  │  - 密码登录  │      │
│  │  - 认证审核 │  │  - 短信登录  │      │
│  │  - 法人验证 │  │  - Token管理│      │
│  └─────────────┘  └─────────────┘      │
│                                          │
│  ┌─────────────┐  ┌─────────────┐      │
│  │ 授权签署模块 │  │ 协议管理模块 │      │
│  │  - 状态检查 │  │  - 协议版本 │      │
│  │  - 协议展示 │  │  - 协议内容 │      │
│  │  - 电子签名 │  │  - 版本管理 │      │
│  │  - 授权存档 │  │             │      │
│  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────┘
```

#### 3.1.2 认证流程技术实现

**登录认证流程：**

```
用户输入手机号+密码
        │
        ▼
  ┌─────────────┐
  │  校验参数   │
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 查询用户信息 │ ←── MySQL
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 密码比对     │ ←── BCrypt加密
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 检查授权状态 │ ←── 查询授权签署记录
  └──────┬──────┘
         │
         ├── 未授权 → 返回"未授权"状态码，前端跳转授权页
         │
         └── 已授权 → 生成Token
                    │
                    ▼
              ┌─────────────┐
              │ 生成JWT Token│
              │ - userId
              │ - enterpriseId
              │ - role
              │ - 过期时间  │
              └──────┬──────┘
                     │
                     ▼
              ┌─────────────┐
              │ 存入Redis   │ ←── 支持单点登录、登出
              └──────┬──────┘
                     │
                     ▼
              返回Token给前端
```

#### 3.1.3 JWT Token设计

```java
// Token载荷结构
{
  "sub": "userId",           // 用户ID
  "enterpriseId": "xxx",     // 企业ID
  "role": "ENTERPRISE",      // 角色
  "authStatus": "SIGNED",    // 授权状态：UNSIGNED/SIGNED
  "exp": 1719388800,         // 过期时间（2小时）
  "iat": 1719381600          // 签发时间
}
```

**Token刷新机制：**
- Access Token：有效期2小时
- Refresh Token：有效期7天
- Token快过期时自动刷新（剩余30分钟触发刷新）

#### 3.1.4 授权签署技术实现

**电子签名方案：**

| 签名方式 | 技术实现 | 适用场景 |
|---------|---------|---------|
| 手写签名 | Canvas + 图片生成 | 移动端友好 |
| 姓名签署 | 输入姓名 + 字体渲染 | 快速签署 |
| 企业公章 | 上传公章图片 + 位置校验 | 企业正式签署 |
| 第三方电子签 | 法大大/e签宝API | 具备法律效力 |

**授权协议版本管理：**
- 协议内容存储在MySQL，富文本格式
- 每次更新生成新版本号（v1.0, v1.1, v2.0）
- 用户签署时记录协议版本号
- 协议重大更新时，已签署用户需重新签署

### 3.2 报告授权管理模块

#### 3.2.1 授权数据模型

```
授权记录 (report_grant)
├── id: 主键
├── grant_enterprise_id: 授权方企业ID
├── grantee_enterprise_id: 被授权方企业ID
├── report_ids: 授权的报告ID列表（JSON数组）
├── grant_scope: 授权范围（ALL / SPECIFIC）
├── grant_purpose: 授权用途
├── grant_time: 授权时间
├── expire_time: 过期时间
├── status: 状态（ACTIVE / EXPIRED / REVOKED）
└── create_time / update_time
```

#### 3.2.2 授权校验流程

```
用户请求查看报告
        │
        ▼
  ┌─────────────┐
  │ 获取用户企业ID│ ←── 从Token解析
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 判断报告归属 │ ←── 查询报告所属企业ID
  └──────┬──────┘
         │
         ├── 本企业报告 → 直接允许查看
         │
         └── 其他企业报告 → 校验授权
                    │
                    ▼
              ┌─────────────┐
              │ 查询授权记录  │
              │ - 授权方=报告所属企业
              │ - 被授权方=当前企业
              │ - 状态=有效
              │ - 未过期    │
              └──────┬──────┘
                     │
                     ├── 存在有效授权 → 允许查看 + 记录查看日志
                     │
                     └── 无有效授权 → 拒绝访问，返回403
```

#### 3.2.3 授权撤销实时生效

使用Redis缓存授权状态，保证撤销立即生效：

```
授权记录变更（创建/撤销/过期）
        │
        ▼
  更新数据库
        │
        ▼
  清除Redis缓存
        │
        ▼
  下次请求重新查询数据库并缓存
```

**缓存Key设计：**
- `grant:{granteeEnterpriseId}:{grantEnterpriseId}` → 授权状态
- 缓存过期时间：1小时
- 授权变更时主动删除对应缓存

### 3.3 报告生成模块

#### 3.3.1 报告生成架构

```
┌──────────────────────────────────────────────────────┐
│                   报告生成流程                         │
├──────────────────────────────────────────────────────┤
│  支付成功回调                                          │
│       │                                               │
│       ▼                                               │
│  ┌─────────────────┐                                  │
│  │  创建生成任务    │ → 写入报告表，状态=GENERATING    │
│  └──────┬──────────┘                                  │
│         │                                              │
│         ▼                                              │
│  ┌─────────────────┐                                  │
│  │  发送MQ消息      │ → RabbitMQ，异步处理              │
│  └──────┬──────────┘                                  │
│         │                                              │
│         ▼                                              │
│  ┌─────────────────┐                                  │
│  │  消费者处理      │                                  │
│  │  1. 采集企业数据  │ ←── 调用企业信息服务             │
│  │  2. 计算信用评分  │ ←── 调用信用评估服务             │
│  │  3. 组装报告内容  │ ←── 模板引擎填充数据              │
│  │  4. 生成PDF文件  │ ←── Flying Saucer / iText       │
│  │  5. 上传到OSS    │ ←── 对象存储                     │
│  │  6. 更新报告状态  │ → 状态=COMPLETED                │
│  └──────┬──────────┘                                  │
│         │                                              │
│         ▼                                              │
│  ┌─────────────────┐                                  │
│  │  发送通知        │ → 站内消息 + 短信 + 邮件          │
│  └─────────────────┘                                  │
└──────────────────────────────────────────────────────┘
```

#### 3.3.2 报告模板引擎技术选型

| 方案 | 优点 | 缺点 | 推荐度 |
|-----|------|------|-------|
| **Thymeleaf + Flying Saucer** | 模板易维护，HTML转PDF | CSS支持有限 | ⭐⭐⭐⭐ |
| **Freemarker + iText** | 性能好，功能强大 | 模板开发复杂 | ⭐⭐⭐⭐ |
| **JasperReports** | 功能全面，可视化设计 | 学习曲线陡 | ⭐⭐⭐ |
| **Vue + Puppeteer截图** | 前后端模板统一 | 资源消耗大 | ⭐⭐⭐ |

**推荐方案**：Thymeleaf + Flying Saucer
- 模板使用HTML + CSS，开发效率高
- 支持CSS2.1，大部分样式可用
- 与前端模板可复用部分样式

#### 3.3.3 报告生成状态机

```
PENDING (待生成)
    │
    │ 支付成功，创建任务
    ▼
GENERATING (生成中)
    │
    ├── 生成成功 → COMPLETED (已完成)
    │
    └── 生成失败 → FAILED (失败)
                    │
                    ├── 自动重试（最多3次）
                    │       │
                    │       └── 重试成功 → COMPLETED
                    │       │
                    │       └── 重试失败 → FAILED_FINAL (最终失败)
                    │
                    └── 用户手动重试 → GENERATING
```

#### 3.3.4 报告水印实现

**水印生成策略：**

```java
// 水印内容
String watermarkContent = String.format(
    "报告编号：%s | 查询企业：%s | 查询时间：%s | 仅供内部使用",
    reportNo,
    enterpriseName,
    queryTime
);

// 水印样式
- 字体：微软雅黑
- 字号：14号
- 颜色：浅灰色（#CCCCCC，透明度30%）
- 角度：-30度倾斜
- 间距：横向间距150px，纵向间距100px
- 覆盖范围：全页覆盖
```

**水印实现方式：**
- PDF生成时直接嵌入水印图层
- 使用iText的PdfStamper添加水印
- 水印在内容图层之下，不影响阅读

### 3.4 支付模块

#### 3.4.1 支付集成架构

```
┌──────────────────────────────────────────────────────┐
│                    支付服务架构                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│   ┌─────────────┐    ┌─────────────┐                │
│   │  微信支付   │    │  支付宝支付  │                │
│   │  适配器    │    │  适配器     │                │
│   └──────┬──────┘    └──────┬──────┘                │
│          │                  │                       │
│          └────────┬─────────┘                       │
│                   │                                 │
│                   ▼                                 │
│          ┌─────────────────┐                        │
│          │   支付统一接口    │ ←── 策略模式          │
│          │  - 统一下单      │                        │
│          │  - 支付查询      │                        │
│          │  - 退款          │                        │
│          │  - 回调处理      │                        │
│          └──────┬──────────┘                        │
│                 │                                   │
│                 ▼                                   │
│          ┌─────────────────┐                        │
│          │   订单服务        │                        │
│          │  - 创建订单      │                        │
│          │  - 状态更新      │                        │
│          │  - 事件通知      │                        │
│          └──────┬──────────┘                        │
│                 │                                   │
│                 └──→ 支付成功 → 触发报告生成         │
│                                                      │
└──────────────────────────────────────────────────────┘
```

#### 3.4.2 支付状态同步机制

**三重保障确保支付状态准确：**

1. **支付回调**：第三方主动通知（主要）
2. **主动查询**：订单超时未支付时主动查询（补偿）
3. **定时对账**：每日凌晨批量对账（兜底）

**订单超时处理：**
- 待支付订单超时时间：30分钟
- 超时自动关闭订单
- 超时前5分钟发送支付提醒（可选）

#### 3.4.3 支付安全

| 安全措施 | 实现方式 |
|---------|---------|
| **签名验证** | 验签第三方回调请求，防止伪造 |
| **金额校验** | 回调金额与订单金额必须一致 |
| **幂等处理** | 同一订单多次回调只处理一次 |
| **防重放** | Nonce + 时间戳，有效期5分钟 |
| **HTTPS** | 全程HTTPS传输 |
| **IP白名单** | 回调接口限制第三方IP段 |

---

## 四、数据库设计

### 4.1 数据库概览

- **数据库类型**：MySQL 8.0
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_unicode_ci
- **引擎**：InnoDB
- **分库分表策略**：初期单库，后期按企业ID哈希分表

### 4.2 核心表设计

#### 4.2.1 用户与认证相关

**表：sys_user（系统用户表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_id | bigint | 20 | 是 | 所属企业ID |
| username | varchar | 64 | 是 | 用户名（手机号） |
| password | varchar | 128 | 是 | 密码（BCrypt加密） |
| real_name | varchar | 64 | 否 | 真实姓名 |
| role_type | tinyint | 1 | 是 | 角色类型：1-法人 2-管理员 |
| status | tinyint | 1 | 是 | 状态：0-禁用 1-正常 |
| last_login_time | datetime | — | 否 | 最后登录时间 |
| last_login_ip | varchar | 64 | 否 | 最后登录IP |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |
| deleted | tinyint | 1 | 是 | 逻辑删除：0-未删 1-已删 |

**索引：**
- idx_enterprise_id (enterprise_id)
- uk_username (username) 唯一索引

---

**表：sys_enterprise（企业表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_name | varchar | 255 | 是 | 企业名称 |
| credit_code | varchar | 64 | 是 | 统一社会信用代码 |
| legal_person | varchar | 64 | 是 | 法定代表人姓名 |
| legal_id_card | varchar | 128 | 否 | 法人身份证号（加密） |
| register_capital | decimal | 18,2 | 否 | 注册资本（万元） |
| establish_date | date | — | 否 | 成立日期 |
| industry | varchar | 64 | 否 | 行业分类 |
| region | varchar | 64 | 否 | 所属地区 |
| address | varchar | 512 | 否 | 注册地址 |
| contact_phone | varchar | 32 | 否 | 联系电话 |
| contact_email | varchar | 128 | 否 | 联系邮箱 |
| auth_status | tinyint | 1 | 是 | 认证状态：0-待审核 1-已认证 2-审核失败 |
| auth_remark | varchar | 512 | 否 | 审核备注 |
| auth_time | datetime | — | 否 | 认证通过时间 |
| member_level | tinyint | 1 | 是 | 会员等级：1-普通 2-银卡 3-金卡 4-钻石 5-企业 |
| balance | decimal | 12,2 | 是 | 本金余额 |
| gift_balance | decimal | 12,2 | 是 | 赠送余额 |
| frozen_amount | decimal | 12,2 | 是 | 冻结金额 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_credit_code (credit_code) 唯一索引
- idx_enterprise_name (enterprise_name)
- idx_auth_status (auth_status)

---

**表：auth_agreement（授权协议表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| version | varchar | 32 | 是 | 协议版本号（v1.0, v2.0） |
| title | varchar | 255 | 是 | 协议标题 |
| content | text | — | 是 | 协议内容（富文本） |
| status | tinyint | 1 | 是 | 状态：0-草稿 1-已发布 2-已下线 |
| publish_time | datetime | — | 否 | 发布时间 |
| create_time | datetime | — | 是 | 创建时间 |
| create_by | bigint | 20 | 是 | 创建人 |

**索引：**
- uk_version (version) 唯一索引

---

**表：auth_sign_record（授权签署记录表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| sign_no | varchar | 64 | 是 | 签署编号（唯一） |
| enterprise_id | bigint | 20 | 是 | 企业ID |
| user_id | bigint | 20 | 是 | 签署人用户ID |
| agreement_id | bigint | 20 | 是 | 协议ID |
| agreement_version | varchar | 32 | 是 | 协议版本 |
| sign_type | tinyint | 1 | 是 | 签名类型：1-手写 2-姓名 3-公章 |
| sign_image | varchar | 512 | 否 | 签名图片URL |
| sign_time | datetime | — | 是 | 签署时间 |
| sign_ip | varchar | 64 | 否 | 签署IP |
| status | tinyint | 1 | 是 | 状态：1-有效 2-已撤回 |
| revoke_time | datetime | — | 否 | 撤回时间 |
| create_time | datetime | — | 是 | 创建时间 |

**索引：**
- uk_sign_no (sign_no) 唯一索引
- idx_enterprise_id (enterprise_id)
- idx_user_id (user_id)

---

#### 4.2.2 报告授权相关

**表：report_grant（报告授权表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| grant_no | varchar | 64 | 是 | 授权编号（唯一） |
| grant_enterprise_id | bigint | 20 | 是 | 授权方企业ID |
| grantee_enterprise_id | bigint | 20 | 是 | 被授权方企业ID |
| grant_scope | tinyint | 1 | 是 | 授权范围：1-全部报告 2-指定报告 |
| report_ids | json | — | 否 | 授权的报告ID列表（JSON数组） |
| report_types | varchar | 128 | 否 | 授权的报告类型（逗号分隔） |
| grant_purpose | varchar | 512 | 否 | 授权用途说明 |
| grant_time | datetime | — | 是 | 授权时间 |
| expire_time | datetime | — | 是 | 过期时间 |
| status | tinyint | 1 | 是 | 状态：1-有效 2-已过期 3-已撤销 |
| revoke_time | datetime | — | 否 | 撤销时间 |
| revoke_reason | varchar | 512 | 否 | 撤销原因 |
| create_by | bigint | 20 | 是 | 创建人（授权方操作人） |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_grant_no (grant_no) 唯一索引
- idx_grant_enterprise (grant_enterprise_id)
- idx_grantee_enterprise (grantee_enterprise_id)
- idx_status (status)
- uk_grant_pair (grant_enterprise_id, grantee_enterprise_id, status) 联合唯一索引（同一对企业只有一个有效授权）

---

**表：report_grant_log（授权操作日志表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| grant_id | bigint | 20 | 是 | 授权记录ID |
| operation_type | tinyint | 1 | 是 | 操作类型：1-创建授权 2-撤销授权 3-续期 4-修改 5-查看报告 |
| operator_id | bigint | 20 | 是 | 操作人ID |
| operator_enterprise_id | bigint | 20 | 是 | 操作人所属企业ID |
| operation_content | varchar | 1024 | 否 | 操作内容描述 |
| operation_ip | varchar | 64 | 否 | 操作IP |
| create_time | datetime | — | 是 | 操作时间 |

**索引：**
- idx_grant_id (grant_id)
- idx_operation_type (operation_type)
- idx_create_time (create_time)

---

#### 4.2.3 企业信息相关

**表：enterprise_info（企业信息表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_name | varchar | 255 | 是 | 企业名称 |
| credit_code | varchar | 64 | 是 | 统一社会信用代码 |
| legal_person | varchar | 64 | 是 | 法定代表人 |
| register_capital | decimal | 18,2 | 否 | 注册资本（万元） |
| paid_capital | decimal | 18,2 | 否 | 实缴资本（万元） |
| establish_date | date | — | 否 | 成立日期 |
| business_term_start | date | — | 否 | 营业期限开始 |
| business_term_end | date | — | 否 | 营业期限结束 |
| enterprise_type | varchar | 64 | 否 | 企业类型 |
| register_authority | varchar | 128 | 否 | 登记机关 |
| approve_date | date | — | 否 | 核准日期 |
| business_status | varchar | 32 | 是 | 经营状态：存续/在业/吊销/注销等 |
| industry_code | varchar | 32 | 否 | 行业编码 |
| industry_name | varchar | 128 | 否 | 行业名称 |
| region_code | varchar | 32 | 否 | 地区编码 |
| region_name | varchar | 128 | 否 | 地区名称 |
| register_address | varchar | 512 | 否 | 注册地址 |
| business_scope | text | — | 否 | 经营范围 |
| contact_phone | varchar | 32 | 否 | 联系电话 |
| contact_email | varchar | 128 | 否 | 联系邮箱 |
| website | varchar | 255 | 否 | 官方网站 |
| office_address | varchar | 512 | 否 | 办公地址 |
| data_source | varchar | 64 | 否 | 数据来源 |
| data_update_time | datetime | — | 否 | 数据更新时间 |
| es_id | varchar | 64 | 否 | Elasticsearch文档ID |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_credit_code (credit_code) 唯一索引
- idx_enterprise_name (enterprise_name) 全文索引（ES）
- idx_industry_code (industry_code)
- idx_region_code (region_code)
- idx_business_status (business_status)

---

#### 4.2.4 信用评估相关

**表：credit_score（信用评分表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_id | bigint | 20 | 是 | 企业ID |
| total_score | int | 11 | 是 | 综合评分（0-1000） |
| credit_rating | varchar | 16 | 是 | 信用等级（AAA/AA/A/BBB/BB/B/CCC/CC/C） |
| model_version | varchar | 32 | 是 | 评估模型版本 |
| basic_score | int | 11 | 是 | 基础素质评分（满分200） |
| operation_score | int | 11 | 是 | 经营能力评分（满分200） |
| finance_score | int | 11 | 是 | 财务状况评分（满分250） |
| credit_record_score | int | 11 | 是 | 信用记录评分（满分200） |
| potential_score | int | 11 | 是 | 发展潜力评分（满分150） |
| industry_rank_percent | decimal | 5,2 | 否 | 行业排名百分位 |
| region_rank_percent | decimal | 5,2 | 否 | 地区排名百分位 |
| score_trend | json | — | 否 | 近6个月评分趋势（JSON数组） |
| key_bonus_items | json | — | 否 | 关键加分项（JSON数组） |
| key_penalty_items | json | — | 否 | 关键扣分项（JSON数组） |
| evaluation_time | datetime | — | 是 | 评估时间 |
| expire_time | datetime | — | 是 | 评分有效期 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- idx_enterprise_id (enterprise_id)
- idx_credit_rating (credit_rating)
- idx_total_score (total_score)
- idx_evaluation_time (evaluation_time)

---

#### 4.2.5 报告相关

**表：credit_report（征信报告表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| report_no | varchar | 64 | 是 | 报告编号（唯一） |
| enterprise_id | bigint | 20 | 是 | 所属企业ID |
| enterprise_name | varchar | 255 | 是 | 企业名称（冗余） |
| credit_code | varchar | 64 | 是 | 统一社会信用代码（冗余） |
| report_type | tinyint | 1 | 是 | 报告类型：1-简版 2-标准 3-深度 4-专项 |
| template_id | bigint | 20 | 是 | 模板ID |
| template_version | varchar | 32 | 是 | 模板版本 |
| score_id | bigint | 20 | 否 | 关联评分ID |
| total_score | int | 11 | 否 | 信用评分（冗余） |
| credit_rating | varchar | 16 | 否 | 信用等级（冗余） |
| generate_status | tinyint | 1 | 是 | 生成状态：1-待生成 2-生成中 3-已完成 4-失败 |
| fail_reason | varchar | 1024 | 否 | 失败原因 |
| retry_count | int | 11 | 是 | 重试次数 |
| file_url | varchar | 512 | 否 | 报告文件URL（OSS） |
| file_size | bigint | 20 | 否 | 文件大小（字节） |
| watermark_config | json | — | 否 | 水印配置 |
| valid_days | int | 11 | 是 | 有效天数 |
| expire_time | datetime | — | 是 | 过期时间 |
| order_id | bigint | 20 | 否 | 关联订单ID |
| owner_enterprise_id | bigint | 20 | 是 | 归属企业ID（购买企业） |
| generate_start_time | datetime | — | 否 | 生成开始时间 |
| generate_end_time | datetime | — | 否 | 生成完成时间 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_report_no (report_no) 唯一索引
- idx_enterprise_id (enterprise_id)
- idx_owner_enterprise_id (owner_enterprise_id)
- idx_generate_status (generate_status)
- idx_create_time (create_time)
- idx_order_id (order_id)

---

#### 4.2.6 订单支付相关

**表：order_info（订单表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| order_no | varchar | 64 | 是 | 订单号（唯一） |
| enterprise_id | bigint | 20 | 是 | 下单企业ID |
| user_id | bigint | 20 | 是 | 下单用户ID |
| order_type | tinyint | 1 | 是 | 订单类型：1-单份报告 2-批量报告 3-套餐 4-会员 5-充值 |
| goods_name | varchar | 255 | 是 | 商品名称 |
| goods_detail | json | — | 否 | 商品详情（JSON：报告类型、数量等） |
| total_amount | decimal | 12,2 | 是 | 订单总金额 |
| discount_amount | decimal | 12,2 | 是 | 优惠金额 |
| coupon_amount | decimal | 12,2 | 是 | 优惠券抵扣金额 |
| pay_amount | decimal | 12,2 | 是 | 实付金额 |
| pay_method | tinyint | 1 | 否 | 支付方式：1-微信 2-支付宝 3-余额 |
| pay_status | tinyint | 1 | 是 | 支付状态：0-待支付 1-已支付 2-已退款 3-部分退款 |
| order_status | tinyint | 1 | 是 | 订单状态：1-待支付 2-已支付 3-已完成 4-已取消 5-退款中 6-已退款 |
| transaction_id | varchar | 128 | 否 | 第三方交易流水号 |
| pay_time | datetime | — | 否 | 支付时间 |
| expire_time | datetime | — | 是 | 订单过期时间 |
| coupon_id | bigint | 20 | 否 | 使用的优惠券ID |
| invoice_status | tinyint | 1 | 是 | 开票状态：0-未开票 1-开票中 2-已开票 3-开票失败 |
| remark | varchar | 512 | 否 | 订单备注 |
| cancel_time | datetime | — | 否 | 取消时间 |
| cancel_reason | varchar | 512 | 否 | 取消原因 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_order_no (order_no) 唯一索引
- idx_enterprise_id (enterprise_id)
- idx_order_status (order_status)
- idx_pay_status (pay_status)
- idx_create_time (create_time)
- idx_transaction_id (transaction_id)

---

**表：payment_record（支付记录表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| order_id | bigint | 20 | 是 | 订单ID |
| order_no | varchar | 64 | 是 | 订单号（冗余） |
| pay_method | tinyint | 1 | 是 | 支付方式：1-微信 2-支付宝 3-余额 |
| pay_amount | decimal | 12,2 | 是 | 支付金额 |
| transaction_id | varchar | 128 | 否 | 第三方交易号 |
| pay_status | tinyint | 1 | 是 | 支付状态：1-支付中 2-成功 3-失败 |
| fail_reason | varchar | 512 | 否 | 失败原因 |
| pay_time | datetime | — | 否 | 支付完成时间 |
| callback_data | text | — | 否 | 回调原始数据（JSON） |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- idx_order_id (order_id)
- idx_transaction_id (transaction_id)
- idx_pay_status (pay_status)

---

**表：refund_record（退款记录表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| refund_no | varchar | 64 | 是 | 退款单号（唯一） |
| order_id | bigint | 20 | 是 | 订单ID |
| enterprise_id | bigint | 20 | 是 | 企业ID |
| refund_amount | decimal | 12,2 | 是 | 退款金额 |
| refund_reason | varchar | 1024 | 是 | 退款原因 |
| refund_type | tinyint | 1 | 是 | 退款类型：1-自动退款 2-人工审核 |
| refund_status | tinyint | 1 | 是 | 状态：1-申请中 2-处理中 3-成功 4-失败 5-拒绝 |
| audit_by | bigint | 20 | 否 | 审核人 |
| audit_time | datetime | — | 否 | 审核时间 |
| audit_remark | varchar | 512 | 否 | 审核备注 |
| refund_time | datetime | — | 否 | 退款到账时间 |
| refund_transaction_id | varchar | 128 | 否 | 退款交易号 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

**索引：**
- uk_refund_no (refund_no) 唯一索引
- idx_order_id (order_id)
- idx_refund_status (refund_status)

---

**表：balance_log（余额流水表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_id | bigint | 20 | 是 | 企业ID |
| change_type | tinyint | 1 | 是 | 变动类型：1-充值 2-消费 3-退款 4-冻结 5-解冻 6-赠送 |
| amount | decimal | 12,2 | 是 | 变动金额（正为加，负为减） |
| balance_after | decimal | 12,2 | 是 | 变动后本金余额 |
| gift_balance_after | decimal | 12,2 | 是 | 变动后赠送余额 |
| related_type | tinyint | 1 | 否 | 关联类型：1-订单 2-退款 3-充值 |
| related_id | bigint | 20 | 否 | 关联ID（订单ID/退款ID） |
| remark | varchar | 512 | 否 | 备注 |
| create_time | datetime | — | 是 | 创建时间 |

**索引：**
- idx_enterprise_id (enterprise_id)
- idx_change_type (change_type)
- idx_create_time (create_time)

---

#### 4.2.7 系统管理相关

**表：sys_role（角色表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| role_code | varchar | 64 | 是 | 角色编码 |
| role_name | varchar | 64 | 是 | 角色名称 |
| description | varchar | 255 | 否 | 角色描述 |
| status | tinyint | 1 | 是 | 状态：0-禁用 1-正常 |
| sort | int | 11 | 是 | 排序 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

---

**表：sys_menu（菜单表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| parent_id | bigint | 20 | 是 | 父级ID，0为顶级 |
| menu_name | varchar | 64 | 是 | 菜单名称 |
| menu_type | tinyint | 1 | 是 | 类型：1-目录 2-菜单 3-按钮 |
| path | varchar | 255 | 否 | 路由路径 |
| component | varchar | 255 | 否 | 组件路径 |
| perms | varchar | 255 | 否 | 权限标识 |
| icon | varchar | 64 | 否 | 图标 |
| sort | int | 11 | 是 | 排序 |
| visible | tinyint | 1 | 是 | 是否显示：0-隐藏 1-显示 |
| status | tinyint | 1 | 是 | 状态：0-禁用 1-正常 |
| create_time | datetime | — | 是 | 创建时间 |
| update_time | datetime | — | 是 | 更新时间 |

---

**表：sys_role_menu（角色菜单关联表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| role_id | bigint | 20 | 是 | 角色ID |
| menu_id | bigint | 20 | 是 | 菜单ID |
| create_time | datetime | — | 是 | 创建时间 |

---

**表：sys_oper_log（操作日志表）**

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|-------|------|------|------|------|
| id | bigint | 20 | 是 | 主键，自增 |
| enterprise_id | bigint | 20 | 否 | 企业ID |
| user_id | bigint | 20 | 是 | 操作人ID |
| username | varchar | 64 | 是 | 操作人账号 |
| module | varchar | 64 | 否 | 模块名称 |
| operation | varchar | 255 | 否 | 操作描述 |
| method | varchar | 255 | 否 | 请求方法 |
| request_params | text | — | 否 | 请求参数 |
| response_result | text | — | 否 | 返回结果 |
| ip | varchar | 64 | 否 | 操作IP |
| location | varchar | 255 | 否 | 操作地点 |
| status | tinyint | 1 | 是 | 状态：1-正常 2-异常 |
| error_msg | text | — | 否 | 错误信息 |
| cost_time | bigint | 20 | 否 | 耗时（毫秒） |
| create_time | datetime | — | 是 | 操作时间 |

**索引：**
- idx_enterprise_id (enterprise_id)
- idx_user_id (user_id)
- idx_create_time (create_time)

---

### 4.3 Redis设计

#### 4.3.1 Key命名规范

| 类型 | 命名格式 | 示例 | 过期时间 |
|-----|---------|------|---------|
| 用户Token | `token:{userId}` | token:10001 | 2小时 |
| 用户信息 | `user:{userId}` | user:10001 | 30分钟 |
| 企业信息 | `enterprise:{enterpriseId}` | enterprise:10001 | 1小时 |
| 授权状态 | `grant:{granteeId}:{grantId}` | grant:20001:10001 | 1小时 |
| 短信验证码 | `sms:{mobile}:{type}` | sms:13800138000:login | 5分钟 |
| 限流计数 | `rate:{key}:{period}` | rate:login:13800138000 | 1分钟 |
| 报告生成进度 | `report:progress:{reportId}` | report:progress:10001 | 1天 |
| 订单防重 | `order:lock:{orderNo}` | order:lock:ORD2024060001 | 10分钟 |

#### 4.3.2 限流设计

```
登录限流：
- 同一手机号：1分钟5次，1小时20次
- 同一IP：1分钟50次

查询限流：
- 同一企业用户：1分钟100次
- 同一企业用户：1天10000次

支付限流：
- 同一订单：1分钟最多3次支付请求
```

### 4.4 Elasticsearch设计

#### 4.4.1 企业信息索引

**索引名**：`enterprise_info`

```json
{
  "mappings": {
    "properties": {
      "id": { "type": "long" },
      "enterpriseName": { 
        "type": "text",
        "analyzer": "ik_max_word",
        "search_analyzer": "ik_smart",
        "fields": {
          "keyword": { "type": "keyword" }
        }
      },
      "creditCode": { "type": "keyword" },
      "legalPerson": { 
        "type": "text",
        "analyzer": "ik_max_word"
      },
      "registerCapital": { "type": "double" },
      "establishDate": { "type": "date" },
      "industryCode": { "type": "keyword" },
      "industryName": { "type": "keyword" },
      "regionCode": { "type": "keyword" },
      "regionName": { "type": "keyword" },
      "businessStatus": { "type": "keyword" },
      "creditRating": { "type": "keyword" },
      "totalScore": { "type": "integer" },
      "dataUpdateTime": { "type": "date" },
      "createTime": { "type": "date" }
    }
  }
}
```

---

## 五、接口设计

### 5.1 接口规范

#### 5.1.1 RESTful API设计

| 规范项 | 约定 |
|-------|------|
| **协议** | HTTPS |
| **数据格式** | JSON |
| **字符集** | UTF-8 |
| **时间格式** | yyyy-MM-dd HH:mm:ss |
| **版本控制** | URL路径版本：/api/v1/xxx |
| **请求方式** | GET（查询）、POST（创建）、PUT（更新）、DELETE（删除） |

#### 5.1.2 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1719388800000
}
```

**状态码规范：**

| 状态码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录 / Token无效 |
| 402 | 数据未授权（未签署数据授权协议） |
| 403 | 无权限 / 未授权查看该报告 |
| 404 | 资源不存在 |
| 429 | 请求过于频繁（限流） |
| 500 | 服务器内部错误 |

#### 5.1.3 分页参数

**请求参数：**
```
pageNum: 1          // 页码，从1开始
pageSize: 10        // 每页条数，默认10
```

**响应格式：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10,
    "pages": 10
  }
}
```

### 5.2 核心接口清单

#### 5.2.1 认证授权接口

| 接口名称 | 请求方式 | URL | 说明 |
|---------|---------|-----|------|
| 企业注册 | POST | /api/v1/auth/register | 企业用户注册 |
| 登录（密码） | POST | /api/v1/auth/login | 手机号密码登录 |
| 登录（短信） | POST | /api/v1/auth/sms-login | 短信验证码登录 |
| 发送验证码 | POST | /api/v1/auth/send-sms | 发送短信验证码 |
| 退出登录 | POST | /api/v1/auth/logout | 退出登录 |
| 刷新Token | POST | /api/v1/auth/refresh | 刷新访问令牌 |
| 检查授权状态 | GET | /api/v1/auth/sign-status | 检查数据授权签署状态 |
| 获取授权协议 | GET | /api/v1/auth/agreement | 获取最新授权协议内容 |
| 签署授权协议 | POST | /api/v1/auth/sign | 签署数据授权协议 |
| 查询签署记录 | GET | /api/v1/auth/sign-record | 查询签署记录 |
| 下载授权凭证 | GET | /api/v1/auth/download-cert | 下载授权凭证PDF |

#### 5.2.2 企业信息接口

| 接口名称 | 请求方式 | URL | 说明 |
|---------|---------|-----|------|
| 企业搜索 | GET | /api/v1/enterprise/search | 搜索企业（名称/信用代码） |
| 本企业信息 | GET | /api/v1/enterprise/my | 获取当前登录企业信息 |
| 企业详情 | GET | /api/v1/enterprise/{id} | 企业详细信息 |
| 股东列表 | GET | /api/v1/enterprise/{id}/shareholders | 企业股东信息 |
| 高管列表 | GET | /api/v1/enterprise/{id}/executives | 企业高管信息 |
| 变更记录 | GET | /api/v1/enterprise/{id}/changes | 工商变更记录 |

#### 5.2.3 报告接口

| 接口名称 | 请求方式 | URL | 说明 |
|---------|---------|-----|------|
| 报告列表 | GET | /api/v1/report/list | 已购买报告列表 |
| 报告详情 | GET | /api/v1/report/{id} | 报告详情 |
| 报告预览 | GET | /api/v1/report/{id}/preview | 报告在线预览 |
| 报告下载 | GET | /api/v1/report/{id}/download | 报告下载 |
| 生成进度 | GET | /api/v1/report/{id}/progress | 报告生成进度 |
| 被授权报告列表 | GET | /api/v1/report/granted-list | 其他企业授权的报告列表 |
| 查看被授权报告 | GET | /api/v1/report/granted/{grantId} | 查看被授权的报告 |

#### 5.2.4 授权管理接口

| 接口名称 | 请求方式 | URL | 说明 |
|---------|---------|-----|------|
| 创建授权 | POST | /api/v1/grant/create | 授权其他企业查看报告 |
| 发出的授权列表 | GET | /api/v1/grant/outbound | 我发出的授权列表 |
| 收到的授权列表 | GET | /api/v1/grant/inbound | 我收到的授权列表 |
| 授权详情 | GET | /api/v1/grant/{id} | 授权详情 |
| 撤销授权 | POST | /api/v1/grant/{id}/revoke | 撤销已发出的授权 |
| 续期授权 | POST | /api/v1/grant/{id}/renew | 延长授权有效期 |
| 修改授权 | PUT | /api/v1/grant/{id} | 修改授权范围 |
| 授权操作日志 | GET | /api/v1/grant/{id}/logs | 授权操作日志 |
| 授权查看日志 | GET | /api/v1/grant/{id}/view-logs | 被授权方查看日志 |

#### 5.2.5 订单支付接口

| 接口名称 | 请求方式 | URL | 说明 |
|---------|---------|-----|------|
| 创建订单 | POST | /api/v1/order/create | 创建报告购买订单 |
| 订单列表 | GET | /api/v1/order/list | 我的订单列表 |
| 订单详情 | GET | /api/v1/order/{id} | 订单详情 |
| 取消订单 | POST | /api/v1/order/{id}/cancel | 取消未支付订单 |
| 发起支付 | POST | /api/v1/pay/create | 发起支付 |
| 支付状态查询 | GET | /api/v1/pay/{orderNo}/status | 查询支付状态 |
| 微信支付回调 | POST | /api/v1/pay/wechat/notify | 微信支付回调 |
| 支付宝回调 | POST | /api/v1/pay/alipay/notify | 支付宝回调 |
| 申请退款 | POST | /api/v1/refund/apply | 申请退款 |
| 退款列表 | GET | /api/v1/refund/list | 退款记录列表 |
| 余额充值 | POST | /api/v1/balance/recharge | 余额充值 |
| 余额明细 | GET | /api/v1/balance/logs | 余额流水明细 |

---

## 六、部署架构

### 6.1 生产环境部署架构

```
                              ┌─────────────┐
                              │   CDN加速    │
                              │ (静态资源)   │
                              └──────┬──────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  负载均衡    │
                              │  (Nginx/LB)  │
                              └──────┬──────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
  ┌──────────────┐          ┌──────────────┐          ┌──────────────┐
  │  网关服务     │          │  网关服务     │          │  网关服务     │
  │  (Gateway)   │          │  (Gateway)   │          │  (Gateway)   │
  └──────┬───────┘          └──────┬───────┘          └──────┬──────┘
         │                           │                           │
         └───────────────────────────┼───────────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
              ┌──────────┐   ┌──────────┐   ┌──────────┐
              │ 用户服务  │   │ 企业服务  │   │ 报告服务  │  ... 各微服务
              │  (多副本) │   │  (多副本) │   │  (多副本) │
              └──────────┘   └──────────┘   └──────────┘
                    │                │                │
                    └────────────────┼────────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
  ┌──────────────┐          ┌──────────────┐          ┌──────────────┐
  │   MySQL主库   │          │   Redis集群   │          │   ES集群     │
  │  (主从复制)   │          │  (哨兵模式)   │          │  (3节点)     │
  └──────────────┘          └──────────────┘          └──────────────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  RabbitMQ   │
                              │  (集群)     │
                              └─────────────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  OSS存储    │
                              │  (阿里云)   │
                              └─────────────┘
```

### 6.2 服务器配置建议（初期）

| 服务 | 配置 | 数量 | 说明 |
|-----|------|------|------|
| **网关服务** | 4核8G | 2台 | 高可用 |
| **认证服务** | 2核4G | 2台 | 高可用 |
| **企业信息服务** | 4核8G | 2台 | 数据查询密集 |
| **信用评估服务** | 4核8G | 2台 | 计算密集 |
| **报告服务** | 4核8G | 2台 | PDF生成CPU密集 |
| **订单支付服务** | 2核4G | 2台 | 事务密集 |
| **授权管理服务** | 2核4G | 2台 | — |
| **风险预警服务** | 2核4G | 2台 | 定时任务多 |
| **系统管理服务** | 2核4G | 2台 | — |
| **MySQL** | 8核16G | 2台 | 主从 |
| **Redis** | 4核8G | 3台 | 哨兵集群 |
| **Elasticsearch** | 8核16G | 3台 | 数据节点 |
| **RabbitMQ** | 2核4G | 3台 | 镜像队列 |
| **Nacos/SkyWalking** | 4核8G | 1台 | 管理服务 |

### 6.3 CI/CD流程

```
代码提交 (Git)
    │
    ▼
代码质量检查 (SonarQube)
    │
    ▼
单元测试 + 代码覆盖率
    │
    ▼
构建Docker镜像
    │
    ▼
推送镜像仓库 (Harbor)
    │
    ▼
部署到K8s测试环境
    │
    ▼
自动化测试
    │
    ▼
人工确认 → 部署到生产环境（灰度发布）
```

---

## 七、安全设计

### 7.1 网络安全

| 安全措施 | 实现方式 |
|---------|---------|
| **HTTPS加密** | 全站HTTPS，TLS 1.3 |
| **WAF防护** | Web应用防火墙，防SQL注入、XSS、CSRF |
| **DDoS防护** | 云服务商DDoS高防 |
| **IP白名单** | 管理后台、回调接口限制IP |
| **安全组** | 云服务器安全组，最小权限原则 |

### 7.2 应用安全

| 安全措施 | 实现方式 |
|---------|---------|
| **认证授权** | JWT + Spring Security |
| **密码加密** | BCrypt强哈希算法 |
| **敏感数据加密** | AES加密存储（身份证号、手机号） |
| **数据脱敏** | 前端展示脱敏，日志脱敏 |
| **接口幂等** | Token机制 + 数据库唯一约束 |
| **SQL注入防护** | MyBatis参数化查询 |
| **XSS防护** | 输入过滤 + 输出转义 |
| **CSRF防护** | Token验证 |
| **文件上传安全** | 类型校验、大小限制、病毒扫描 |

### 7.3 数据安全

| 安全措施 | 实现方式 |
|---------|---------|
| **数据备份** | 每日全量备份 + 实时增量备份 |
| **异地容灾** | 跨可用区部署，数据异地备份 |
| **数据删除** | 逻辑删除 + 定期物理清理 |
| **隐私保护** | 符合《个人信息保护法》要求 |
| **审计日志** | 关键操作全记录，不可篡改 |

---

## 八、性能优化

### 8.1 前端性能优化

1. **资源压缩**：Gzip/Brotli压缩
2. **CDN加速**：静态资源CDN分发
3. **懒加载**：图片懒加载、路由懒加载
4. **缓存策略**：浏览器缓存、Service Worker
5. **代码分割**：按需加载，减小首屏包体积
6. **虚拟列表**：长列表使用虚拟滚动

### 8.2 后端性能优化

1. **数据库优化**：
   - 合理建立索引，避免全表扫描
   - SQL优化，避免慢查询
   - 读写分离，读操作走从库

2. **缓存优化**：
   - 热点数据Redis缓存
   - 多级缓存（本地缓存 + Redis）
   - 缓存预热、缓存雪崩/击穿/穿透防护

3. **异步处理**：
   - 报告生成异步化（MQ）
   - 通知发送异步化
   - 日志异步写入

4. **连接池优化**：
   - 数据库连接池合理配置
   - HTTP连接池复用

### 8.3 性能监控指标

| 指标 | 目标值 |
|-----|-------|
| 页面首屏加载 | < 2秒 |
| API响应时间（P95） | < 200ms |
| 数据库查询（P95） | < 100ms |
| 简版报告生成 | < 5秒 |
| 标准版报告生成 | < 30秒 |
| 系统可用性 | > 99.9% |

---

## 九、技术风险与应对

| 风险点 | 风险等级 | 应对方案 |
|-------|---------|---------|
| **企业数据源不稳定** | 高 | 接入多家数据源，互为备份；建立数据质量监控 |
| **报告生成性能瓶颈** | 中 | 异步生成 + 消息队列削峰；PDF生成服务横向扩展 |
| **支付安全风险** | 高 | 多重签名校验；幂等处理；实时对账；人工复核 |
| **数据合规风险** | 高 | 严格的数据授权机制；脱敏处理；合规审计 |
| **大流量冲击** | 中 | 限流降级；缓存预热；弹性扩容 |
| **微服务复杂度** | 中 | 初期可合并部分服务，后期再拆分；完善监控和链路追踪 |

---

## 十、技术文档清单

| 文档名称 | 说明 | 阶段 |
|---------|------|------|
| 架构设计文档 | 系统整体架构设计 | 设计阶段 |
| 数据库设计文档 | 详细表结构设计 | 设计阶段 |
| 接口文档 | API接口详细说明 | 开发阶段 |
| 部署运维文档 | 部署、监控、运维手册 | 上线前 |
| 开发规范文档 | 代码规范、命名规范 | 开发阶段 |

---

*文档结束*

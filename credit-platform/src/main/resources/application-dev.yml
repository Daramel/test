spring:
  datasource:
    url: jdbc:mysql://localhost:3306/credit_platform_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123

  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0

logging:
  level:
    com.credit: debug
    com.credit.mapper: debug
